<Macro Proxy80 $domain $alias $protocol $target $port>
    <VirtualHost *:80>
        # Domains
        ServerName $domain
        ServerAlias $alias
        ProxyPreserveHost On
        ProxyPass / $protocol$target:$port/
        ProxyPassReverse / $protocol$target:$port/
    </VirtualHost>
</Macro>